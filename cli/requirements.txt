# CLI client dependencies for Logos
click>=8.1.0
httpx>=0.24.0
pydantic>=2.0.0

# LLM provider support
anthropic>=0.40.0
openai>=1.0.0
google-generativeai>=0.8.0
# Add other LLM providers as needed (DeepSeek, etc.)

# MCP client for connecting to Logos server
mcp>=0.1.0  # If available, otherwise use httpx for direct API calls